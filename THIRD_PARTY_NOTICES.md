# Third-party notices

The authored sample assets under `Assets/ShaderGraphTechniques` are original procedural or code-generated content and are released under the repository MIT license.

The repository references Unity packages through `Packages/manifest.json` and `Packages/packages-lock.json`. Unity Editor, Universal Render Pipeline, Shader Graph, and their package code are not relicensed by this repository. Their use remains subject to Unity's applicable terms and package licenses.

No external texture, mesh, font file, paid asset, or third-party Shader Graph framework is redistributed by the sample.

The generated public showcase video uses `Digital Clouds` by Alejandro Magaña (A. M.), downloaded from Mixkit and used under the Mixkit Stock Music Free License. The original MP3 is retained only as a local build input under the ignored `Artifacts/SourceAudio/` directory and is not redistributed as a standalone music file. See `Documentation/MUSIC.md` for the source URL, license URL, and file hash. The music is not covered by this repository's MIT license.
